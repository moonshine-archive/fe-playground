import { css } from '@emotion/react'

export const row = {
  layout: css({
    position: 'relative',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: '16px'
  })
}
